{% extends 'base.html.twig' %}
{% block title %}{{ 'participant.edit'|trans }} · {{ app_name }}{% endblock %}
{% block body %}
    <h1 class="title">{{ 'participant.edit'|trans }} — {{ participant.displayName }}</h1>
    {{ include('participant/_form.html.twig') }}
{% endblock %}
