{% extends 'base.html.twig' %}
{% block title %}{{ 'participant.new'|trans }} · {{ app_name }}{% endblock %}
{% block body %}
    <h1 class="title">{{ 'participant.new'|trans }} — {{ participatingDojo.displayName }}</h1>
    {{ include('participant/_form.html.twig') }}
{% endblock %}
