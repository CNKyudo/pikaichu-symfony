{{ form_start(form, {attr: {'data-controller': 'identity-autofill'}}) }}
{{ form_row(form.kyudojin, {attr: {'data-identity-autofill-target': 'source', 'data-action': 'change->identity-autofill#fill'}}) }}
{{ form_row(form.firstname, {attr: {'data-identity-autofill-target': 'firstname'}}) }}
{{ form_row(form.lastname, {attr: {'data-identity-autofill-target': 'lastname'}}) }}
{{ form_row(form.club) }}
{% if form.index is defined %}{{ form_row(form.index) }}{% endif %}

<div class="field is-grouped">
    <div class="control"><button class="button has-background-light" type="submit">{{ 'action.save'|trans }}</button></div>
    <div class="control">
        <a class="button"
           href="{{ path('app_participating_dojo_edit', {taikaiId: taikai.id, id: participatingDojo.id}) }}">
            {{ 'action.back'|trans }}
        </a>
    </div>
</div>
{{ form_end(form) }}
