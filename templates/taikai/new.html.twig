{% extends 'base.html.twig' %}
{% block title %}{{ 'taikai.new'|trans }} · {{ app_name }}{% endblock %}
{% block body %}
<div class="columns">
    <div class="column is-half is-offset-2">
        <div class="card">
            <div class="card-content">
                <p class="title">{{ 'taikai.new'|trans }}</p>
                {{ include('taikai/_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
